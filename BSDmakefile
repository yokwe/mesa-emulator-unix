#
# BSDmakefile
#


SHELL != which bash
.export-env SHELL

HOSTNAME != uname -n
.export-env HOSTNAME

LOG4CPP_CONFIG := tmp/cmake/${HOSTNAME}/run/debug.properties
.export-env LOG4CPP_CONFIG

# include common part
include Makefile
